---
name: te-model-selector
description: |
  智能识别用户数据分析意图，推荐最合适的 TE（数数科技/ThinkingEngine）分析模型，并基于真实埋点元数据生成精确配置。自动处理字段缺失、模型能力边界等问题，必要时提供 SQL 替代方案。

  【意图强校验：模型选择求助】仅当用户描述了某个数据分析场景，并且明确表达了对“模型选择”的“疑问、纠结或求助”意图时才触发。
    [强触发词]（必须包含以下之一才触发）：                                                               
       - “用什么模型”、”不知道用哪个模型”、”该选哪个”、”求推荐模型”                                         
       - “用事件分析还是XX分析”、”这个需求怎么配最合适”                                                     
       - “用哪个分析”、”不知道用留存还是漏斗”                                                               
       - 显式提及具体模型名称并寻求建议（如”留存分析能做这个吗”、”漏斗适合这个需求吗”）
    [排他条件 (绝对不触发)]：                                                                            
      1. 简单数据查询请求（无模型选择关键词）：”查一下XX数据”、”帮我查XX”、”统计XX”、”看XX指标” → 不触发   
      2. 明确指令要求执行特定业务排查（如”查游戏通胀”、”查大R画像”） → 转交业务Skill                       
      3. 直接要求写代码（如”帮我写段SQL”） → 转交SQL/代码类Skill                                           
      4. 明确要求设计埋点表 → 转交埋点设计Skill                                                            
      5. 已知模型名称的配置请求（如”帮我配置留存分析”） → 若无选择纠结意图，直接按用户指定的模型处理
    [关键区分]：                                                                                         
       - “帮我查一下充值数据” → 不触发（纯数据查询，无模型选择求助）                                        
       - “分析充值数据用什么模型” → 触发（有模型选择意图）                                                  
       - “最近7天的DAU” → 不触发（明确指标，无选择求助）                                                    
       - “DAU和MAU用什么模型分析” → 触发（有选择疑问）

---


# TE 分析模型选择 & 搭建助手

根据用户的数据分析需求，智能推荐最合适的 TE（数数科技/ThinkingEngine）分析模型，并结合实际埋点数据字典，提供精确到具体事件和属性的保姆级配置指南。

--- 

# 🔄 核心工作流程

## 阶段 1：意图识别与元数据准备

### 1.1 确认 projectId

- 默认使用当前项目`projectId`
- 用户未提供或未勾选项目时应主动询问：`请提供 TE 项目 ID（projectId）`
- 不猜测、不自动探索

### 1.2 读取埋点元数据（强制执行，不允许跳过除非没有元数据）

在生成配置建议前，**必须**调用工具查询真实的埋点字典，明确区分以下三种数据类型并正确映射，绝不能凭空捏造英文事件名或属性名。

**事件 (Event)**：用户的具体行为（如 register, login）。

**事件属性 (Event Property) & 公共事件属性**: 描述事件发生的具体环境或数值（如金额、渠道）。注：公共事件属性可适用于所有事件的筛选与分组。

**用户属性 (User Property)/ 标签 / 分群**: 描述用户自身的固有静态特征（如 VIP等级、累积充值金额）。

```
调用工具：
- `project_listEvent` (thinkingengine-mcp) → 获取项目中所有已埋点的事件及其中英文名称
- `project_loadFiltProps`(thinkingengine-mcp) → 获取全量属性（含类型、描述、tableType）
- `project_listMetricInfos` (thinkingengine-mcp) → 获取预定义指标（可选），查看项目中已预定义的指标/公式，如果用户需求刚好有现成指标/标签/分群等，可以复用。
```

### 1.3 意图解析与字段映射
- 将用户口语（"充值金额"、"登录"）映射到真实字段（`pay_amount`、`login`）
- 区分事件属性（tableType=0）和用户属性（tableType=1）
- 确认属性类型（string/number/bool/date/datetime/array）以正确配置筛选和分组
- 精确匹配优先，模糊推理兜底，歧义必须确认

### 元数据映射原则

- **精确匹配优先**：用户说"充值金额"，先在属性列表中搜索 propDesc 包含"充值金额"的属性
- **模糊推理兜底**：如果没有精确匹配，展示最相关的候选属性让用户确认
- **歧义必须确认**：当事件属性和用户属性存在同名/同描述属性时，**必须询问用户使用哪个**，不能自动选择
- **找不到就说明**：如果项目中没有匹配的事件或属性，明确告知用户"当前项目未埋点该事件/属性"，并建议补充埋点

---

## 阶段 2：模型判定与配置生成

### 2.1 根据决策树判定模型
按下方【模型判定决策树】和【TE 分析模型体系介绍】选择最合适的分析模型

### 2.2 生成配置（基于真实字段）
判定完成后，按指定格式输出推荐模型，根据【各模型配置方法】输出配置建议。
- 将**映射后的真实字段**填入配置模板
- 检查必需字段是否齐全


#### 📋 输出格式

```
## 📊 推荐模型：[模型中文名] ([模型英文标识])

### 💡 推荐理由
简明扼要地解释为什么选择这个模型，指出命中了哪条决策规则。

### 🛠️ 在 TE 中的配置建议
**(Agent 内部指令：请严格按照以下结构，将自然语言需求转化为对应的底层字段和计算逻辑。字段格式输出为“中文显示名 (英文标识符)”)**
根据判定的模型，根据不同模型的配置方法，使用下方配置模版输出。

**配置模版**：（以事件模型为例，根据不同模型的配置方法动态调整）

| **配置项** | **选择** |
| :--- | :--- |
| **选择事件** | 登录（login） |
| **计算方法** | 触发用户数 |
| **分组项** | 渠道（channel） |
| **时间范围** | 最近7天 |
| **时间粒度** | 按天（可选，看每日趋势）或合计 |
| …… | …… |

### 🔄 备选方案** (如有):
[说明在什么条件下应改用备选模型]

### ⚠️ 注意事项:
[提示该场景下的常见误区，或者埋点数据中需要特别注意的口径问题（如：充值金额不包含赠送部分等）]
```

---

### 2.3 异常处理机制

#### 情况 A：字段缺失
**检测**：配置所需字段在元数据中不存在
**处理**：
1. 提示用户：`当前项目缺少 [字段名]，无法完成配置`
2. 提供替代方案：
   - 推荐相似字段替代
   - 建议补充埋点（给出埋点设计建议）
   - 建议创建虚拟属性/用户标签/分群

**示例**：
```
❌ 缺少字段：用户等级（user_level）
✅ 替代方案：
  1. 使用现有字段 vip_level 替代
  2. 或创建用户标签：基于累计充值金额划分等级
  3. 或补充埋点：在用户属性中添加 user_level
```

#### 情况 B：模型能力边界
**检测**：用户需求超出所选模型能力范围
**处理**：
1. 说明模型局限性
2. 提供 SQL 替代方案（基于 Trino 语法）

**示例**：
```
⚠️ 模型局限：留存分析无法按小时粒度统计
✅ SQL 替代方案：
SELECT
  date_trunc('hour', "$part_time") AS hour,
  COUNT(DISTINCT "#user_id") AS user_count
FROM ta.v_event_1
WHERE "$part_event" = 'login'
  AND "$part_date" >= '2024-01-01'
GROUP BY 1
ORDER BY 1
```
**字段说明**：
- `"$part_date"`：分区日期
- `"$part_event"`：事件名
- `"#user_id"`：用户ID
- `"[真实属性名]"`：[属性描述]

#### 情况 C：需要前置数据准备
**检测**：配置需要用户标签/分群/虚拟属性但尚未创建
**处理**：
1. 提示用户先创建前置数据
2. 给出创建步骤和配置建议

**示例**：
```
⚠️ 需要前置准备：高价值用户分群
✅ 创建步骤：
  1. 进入 TE → 用户分群 → 新建分群
  2. 筛选条件：累计充值金额 >= 1000
  3. 保存为"高价值用户"
  4. 在事件分析中使用该分群进行对比
```

---

## TE 分析模型体系介绍（共11个模型）

### 1. 事件分析 (event) — 默认/最常用模型

**核心定位**: 对单一行为事件进行指标统计和趋势观察，是最基础、最核心的分析模型。当没有明确信号指向其他模型时，默认使用事件分析。

**支持的聚合指标**: 总次数、触发用户数（DAU/MAU）、人均次数、累计值（如总流水）、最大值/最小值、去重数、人均值、均值、公式指标（自定义公式组合多指标）

**核心功能**:
- 支持多指标同时查看
- 支持按维度分组对比（如按渠道、地区、设备类型）
- 支持多日期对比
- 支持筛选条件（事件属性、用户属性筛选）

**强信号关键词**: DAU、MAU、人数、次数、金额、收入、数量、总和、总数、统计、查询、计算、趋势、报表、看板、付费率（单独出现时）、首日付费率、活跃、新增、注册、登录、充值、流水、ARPU、ARPPU、人均

**典型问题**: 最近7天的DAU / 查询最近30天的收入 / 不同渠道的付费用户数 / ARPU分析 / 帮忙生成最近7天注册人数、登录人数、付费金额的看板

---

### 2. 留存分析 (retention) — 第二常用模型

**核心定位**: 分析完成某项初始事件的用户在后续时间段内的回访情况。量化用户的留存与流失，衡量产品健康度。也是 LTV 和 ROI 的计算载体。

**核心概念**:
- **初始事件**: 用户的"起点"行为（如注册、首次登录）
- **回访事件**: 用户的"回访"行为（如再次登录、再次付费）
- **留存率**: 第 N 天/周/月回访的用户占初始用户的比例

**留存类型**: N日留存、N周/N月留存、无界留存、自定义留存

**扩展功能**: 同时展示 LTV（用户生命周期价值）和 ROI（投资回报率）

**强信号关键词**: 留存、留存率、LTV、生命周期价值、ROI（N日ROI/计算ROI）、流失、流失率、流失用户、流失人数、次日留存、N日留存、回访、衰减、次留、三留、七留

**⚠️ 核心规则: LTV 永远是 retention，流失永远是 retention，ROI 默认是 retention**

**典型问题**: 查询这个月的用户留存 / 最近7天的LTV / 最近10天的7日ROI / 7日流失用户 / 付费用户30天留存

---

### 3. 漏斗分析 (funnel)

**核心定位**: 分析多步骤流程中各步的转化及流失情况，找到优化方向。

**核心配置**: 最多支持30个步骤，窗口期1分钟~180天，支持有序/无序漏斗。

**强信号关键词**: 漏斗、漏斗分析、转化（多步骤语境）、先...后/再...、A→B→C、渗透率、转化率（多步骤时）

**⚠️ 关键区分**:
- "付费率"单独出现 → **event**（单一指标）
- "首日付费率" → **event**（单一计算指标）
- "渗透率" → **funnel**（暗含A到B的转化过程）
- "先A再B" + 无其他模型关键词 → **funnel**
- "先A后B" + "留存" → **retention**（显式模型关键词优先）

**典型问题**: 先登录再抽卡再付费的漏斗转化 / 分渠道生成注册→登录→付费的转化 / 新增首日付费渗透率

---

### 4. 间隔分析 (interval)

**核心定位**: 分析用户完成两个具有前后因果关系的事件之间的时间间隔分布。回答"用户完成两件事之间要花多长时间"。

**核心指标**: 中位数、平均值、分位数（P75/P90）、时长分布

**强信号关键词**: 间隔、间隔分析、间隔时间、间隔分布（"间隔"是最强信号，几乎100%命中）

**⚠️ 与漏斗的区分**: 漏斗关注"有多少人转化"，间隔分析关注"转化花了多久"。

**典型问题**: 首次付费间隔分布 / 升级到付费的间隔 / 最近一个月登录间隔

---

### 5. 分布分析 (scatter/distribution)

**核心定位**: 对指标数值进行区间划分，获取每个区间内的用户数量和占比。剖析用户针对某功能的参与深度和粘性。

**分布维度**: 按次数分布、按天数分布、按数值属性分布

**强信号关键词**: 分布、分布情况、分布分析、用户分布（"分布"几乎是决定性信号）

**⚠️ 与事件分析的区分**:
- "付费金额趋势" → **event**（时间序列聚合）
- "付费金额分布" → **scatter**（用户级别分布）
- "每日XX分布" → **scatter**（"分布"优先级高于"每日"）

**典型问题**: 用户登录次数分布 / 付费额度分布 / 活跃天数分布 / 付费次数分布趋势

---

### 6. 路径分析 (path)

**核心定位**: 探索用户行为轨迹，生成桑基图（Sankey Diagram），直观展示行为流入流出。适合发现"意料之外的行为模式"。

**分析方向**: 正向路径（某事件后做了什么）、反向路径（某事件前做了什么）、全路径

**强信号关键词**: 路径、路径分析、行为轨迹、桑基图、流入流出、用户路径、做了什么

**典型问题**: 用户进入游戏后做什么 / 流失用户离开前的行为 / 付费前的行为路径

---

### 7. 属性分析 (composition)

**核心定位**: 基于用户属性（而非行为事件）进行画像分析，支持双维度交叉分析和人群对比。回答"用户是谁/用户长什么样"。

**强信号关键词**: 属性分析、用户画像、属性分布（当分析对象是静态用户属性而非行为指标时）

**⚠️ 与分布分析的区分**:
- **属性分析**: 分析的是静态用户属性（等级、地区、设备类型）的分布
- **分布分析**: 分析的是行为指标（付费次数、登录次数、付费金额）在用户间的分布

**典型问题**: 用户地域分布 / 付费用户的年龄段和设备品牌分布 / VIP用户和普通用户的画像差异

---

### 8. 归因分析 (attribution)

**核心定位**: 评估多个触点/渠道对转化目标的贡献程度。支持末次触达、首次触达、线性、位置衰减、时间衰减等归因模型。

**强信号关键词**: 归因、归因分析、触点贡献、渠道贡献、贡献度、哪个触点、哪个广告位贡献最大

**⚠️ 与漏斗分析的区分**: 漏斗看一条固定流程的转化率，归因分析看多条路径中各触点的贡献度。

**典型问题**: 哪个广告位对付费转化贡献最大 / 推广渠道贡献评估 / 多个弹窗对转化的影响

---

### 9. 排行榜 (rank)

**核心定位**: 按指标值大小对维度进行排名，展示 Top N 榜单，可追踪名次变化。

**强信号关键词**: TOP、Top N、排行、排行榜、最高的是哪个（+分析维度）

**⚠️ 关键区分**:
- "充值最高的**游戏**是哪个" → **rank**（排行对象是游戏，可作为分组维度）
- "充值最高的**用户**" → **other**（查找特定用户需要用户分群或SQL）

**典型问题**: 最近7日付费top5游戏 / 收入Top10渠道排名 / 活跃度最高的Top20服务器

---

### 10. 热力地图 (heatmap)

**核心定位**: 在游戏地图或应用界面上灵活展示各区域的用户热力分布，游戏行业特有需求。

**强信号关键词**: 热力图、热力地图、热点区域、坐标分布、位置分布、地图热度

**典型问题**: 游戏地图中玩家聚集/战斗/死亡的热点区域 / 不同地图区域的玩家分布密度

---

### 11. SQL 查询 (sql)

**核心定位**: 通过 SQL 直接查询底层数据，是分析体系的"万能后手"，满足以上模型无法覆盖的自定义需求。

**适用场景**: 跨模型的复杂计算、特殊统计口径、临时数据探查验证、需要 JOIN 多张表的分析

**典型问题**: 帮我查余额大于1000的用户 / 跨模型的复杂计算 / 需要特殊统计口径的指标

---


## 模型判定决策树（按优先级从高到低执行）

在判定时，严格按以下顺序逐条检查，**第一个命中的规则即为最终结果**：

```
步骤1: 用户是否明确提到了模型名称？
  → 用户说"漏斗分析" → FUNNEL
  → 用户说"留存分析" → RETENTION
  → 用户说"间隔分析" → INTERVAL
  → 用户说"分布分析" → DISTRIBUTION
  → 用户说"路径分析" → PATH
  → 用户说"属性分析" → COMPOSITION
  → 用户说"归因分析" → ATTRIBUTION
  （显式模型名称最高优先级，直接使用）

步骤2: 包含"间隔"（时间间隔）关键词？
  → YES → INTERVAL

步骤3: 包含"漏斗"关键词，或明确的多步骤转化描述？
  判断条件：出现"漏斗"、"先A再B再C的转化"、"A→B→C"
  → YES → 如果同时包含"留存" → RETENTION
          → 否则 → FUNNEL

步骤4: 包含"留存" / "LTV" / "流失"？
  → YES → RETENTION

步骤5: 包含"ROI"？
  → YES → RETENTION（ROI在TE中是留存模型的派生指标）

步骤6: 包含"分布"？
  → YES → 分析对象是静态用户属性（等级、地区、年龄等固有属性）？
          → YES → COMPOSITION（属性分析）
          → NO → DISTRIBUTION（分布分析）

步骤7: 包含"TOP" / "排行" / "最高的是哪个"？
  → YES → 排行对象是分析维度（游戏/渠道/商品等）？
          → YES → RANK
          → 排行对象是找特定用户？
          → YES → OTHER

步骤8: 包含"路径" / "行为轨迹" / "桑基图" / "做了什么"？
  → YES → PATH

步骤9: 包含"归因" / "触点贡献" / "渠道贡献度"？
  → YES → ATTRIBUTION

步骤10: 包含"热力图" / "热点区域" / "坐标分布"？
  → YES → HEATMAP

步骤11: 用户筛选/导出/列表需求？或预测类？或无法归类？
  → YES → OTHER / SQL

步骤12: 以上均不命中？
  → EVENT（事件分析是默认模型）
```

---

## 边缘案例和歧义消解规则（必须严格遵守）

以下是从500+真实案例中总结的核心消歧规则：

| # | 场景 | 正确判定 | 原因 |
|---|------|----------|------|
| 1 | "付费率"单独出现 | event | 单一计算指标，不是多步骤转化 |
| 2 | "首日付费率" | event | 虽然"率"暗示转化，但是单一指标 |
| 3 | "渗透率" | funnel | 暗含从A到B的转化过程 |
| 4 | "付费金额趋势" | event | 时间序列聚合 |
| 5 | "付费金额分布" | distribution | 用户级别分布 |
| 6 | "每日XX分布" | distribution | "分布"优先级 > "每日" |
| 7 | "充值最高的用户" | other | 查找特定用户，不是排行榜 |
| 8 | "充值最高的游戏" | rank | 游戏是分析维度 |
| 9 | "先A后B" + 无模型关键词 | funnel | 顺序动作暗示转化 |
| 10 | "先A后B" + "留存" | retention | 显式模型关键词优先 |
| 11 | LTV（任何形式） | retention | 100%规则，无例外 |
| 12 | 流失（任何形式） | retention | 流失是留存的反面 |
| 13 | ROI（任何形式） | retention | TE通过留存模型计算ROI |
| 14 | 用户列表/筛选/分群需求 | other | 不是分析模型的范畴 |
| 15 | 预测类需求 | other | TE不支持预测分析 |
| 16 | "人次分布" / "人数分布" | distribution | "分布"关键词决定 |
| 17 | "登录人数每日分布" | distribution | "分布" > "每日" |
| 18 | 单纯问某个事件的用户数/次数 | event | 最基础的事件分析 |

---


## 各模型配置方法

### 【事件分析 (event) 配置方法】

#### 前置数据映射要求 (Agent 必读):
在配置前，必须查阅底层数据字典，明确区分以下三种数据类型并正确映射：
- 事件 (Event)：用户的具体行为（如 register, login）。
- 事件属性 (Event Property) & 公共事件属性: 描述事件发生的具体环境或数值（如金额、渠道）。注：公共事件属性可适用于所有事件的筛选与分组。
- 用户属性 (User Property) / 用户标签 / 用户分群: 描述用户自身的固有静态特征（如 VIP等级、累积充值金额）。

```
## 分析指标 (支持多指标组合与公式)
(针对用户的每个数据需求，配置相应的指标线。支持以下三种模式)
### 模式 A：基础事件聚合
选择事件：[匹配到的真实事件，例：注册 (register)]
计算方法：选择 [总次数 / 触发用户数 / 人均次数]
### 模式 B：事件属性聚合 (针对数值型属性)
选择事件：[匹配到的真实事件，例：充值 (payment)]
选择属性：[匹配到的事件属性，例：充值金额 (pay_amount)]
计算方法（事件属性的类型不同，提供的计算方法也不同）：
  - 若事件属性为数值类型，只能选择其中之一： [总和 / 均值 / 人均值 / 中位数 / 最大值 / 最小值 / 去重数 / 方差 / 标准差 / 99分位数 / 95分位数 / 90分位数 / 80分位数 / 75分位数 / 70分位数 / 60分位数 / 40分位数 / 30分位数 / 25分位数 / 20分位数 / 10分位数 / 5分位数]
  - 若事件属性为文本/字符串或时间，只能选择[去重数]
  - 若事件属性为布尔，只能选择其中之一：[为真数 / 为假数 / 为空数 / 不为空数 / 去重数]
  - 若事件属性为对象/对象组，只能选择其中之一：[为空数 / 不为空数 / 去重数]
### 模式 C：复合指标 (自定义公式，支持加减乘除)
公式逻辑：使用上述 A 或 B 模式构建的基础指标进行四则运算。
示例配置：[指标A: 充值 (payment) 的 触发用户数] 除以 (/)  [指标B: 登录 (login) 的 触发用户数]
事件级筛选：公式中的事件支持事件级筛选，筛选条件 「事件属性/用户属性/用户分群/用户标签」「逻辑运算符」「具体值」
事件级筛选示例配置：[指标A: 充值 (payment) 渠道 = 官网 的 触发用户数] 除以 (/)  [指标B: 登录 (login) 的 触发用户数]

📌 筛选条件的层级说明：
｜筛选类型｜作用范围｜使用场景｜
｜全局筛选｜所有指标｜适用于所有指标的统一筛选条件｜
｜指标级筛选｜当前单一指标｜仅针对特定指标的筛选条件，其他指标不受影响｜
｜事件级筛选｜公式中的某个事件｜仅在复合指标（自定义公式）的事件里可用｜
示例：查询"深圳市不同设备类型的用户最近30天人均金币消耗次数、V1.0版本的总抽卡人数"
全局筛选：城市 = 深圳（所有指标都适用）
指标级筛选：版本 = V1.0（仅抽卡指标适用，人均金币消耗次数不受此筛选影响）

🔍 指标级筛选 (选填，可添加多个，逻辑关系且/或，仅作用于当前单一指标):
如果某指标需要单独设置筛选条件（如"V1.0版本的总抽卡人数"），需在此指标下单独添加筛选：[属性名] [逻辑运算符，如：等于/不等于/包含] [具体值]。
🔍 事件级筛选（选填，可添加多个，逻辑关系且/或，只能在使用复杂指标（自定义公式）的事件里才能用

## 全局筛选 (选填，可添加多个，逻辑关系且/或，作用于整个图表）
（用于圈定整体分析的样本范围)
选择过滤条件：[匹配到的 事件属性 / 公共事件属性 / 用户属性 / 用户标签 / 用户分群]
选择逻辑运算符：
  - 若过滤条件为文本类型，可以选择[等于 / 不等于 / 包括 / 不包括 / 有值 / 无值 / 正则匹配 / 正则不匹配]
  - 若过滤条件为时间类型，可以选择[位于区间 / 小于等于 / 大于等于 / 相对当前日期 / 相对事件发生时刻 / 有值 / 无值]
  - 若过滤条件为数值类型，可以选择[等于 / 不等于 / 小于 / 小于等于 / 大于 / 大于等于 / 有值 / 无值 / 区间]
  - 若过滤条件为布尔类型，可以选择[为真 / 为假 / 有值 / 无值]
设定具体值：[具体数值或字符串]
示例：「渠道」「等于」「官网」；「level」「小于等于」「100」；「is_first_login」「为真」

## 分组项 (选填，可添加多个分组项，用于拆解对比)
(将大盘数据按特定维度拆分，观察多条趋势线)
按维度分组：选择 [匹配到的 事件属性 / 公共事件属性 / 用户属性 / 用户标签 / 用户分群]
示例：按 用户价值分层 (user_value_level) 分组

## 时间范围与粒度
时间范围：根据匹配到的时间范围选择 [例如：过去 7 天 / 本月]
时间粒度：按 [天 / 小时 / 周 / 月 / 分钟（1分钟/5分钟/10分钟）/ 合计] 展现
```
--- 

### 【留存分析 (retention) 配置方法】

#### 前置数据映射要求 (Agent 必读):
配置留存模型前，必须查阅底层数据字典，并严格遵守以下映射关系：
- 初始事件与回访事件：必须映射为真实的事件 (Event)。
- 属性层级约束：分组时若使用事件属性，必须且只能使用「初始事件」的事件属性（或公共事件属性/用户属性）。绝对不可将回访事件的属性用于外层分组！

```
## 分析主体
选择计算的视角：[匹配到的真实分析主体，例：用户 (user_id) / 角色 (role_id)/ 访客（distinct_id），若没有提示用户创建]

## 核心事件配置
▶️ 初始事件 (起点)：选择 [匹配到的真实事件，例：注册 (register)]
🔙 回访事件 (目标)：选择 [匹配到的真实事件，例：登录 (login)]

## 使用同时展示 (进阶选填，计算留存用户的额外行为或LTV)
(针对已经完成「回访事件」的用户，计算他们在另一指定事件上的表现)
- 参与事件：[匹配到的事件，例：登录 (login) 或 充值 (payment)]
- 分析指标 (二选一)：
  1.基础预置指标：[总次数 / 触发用户数 / 人均次数 / 总次数的周期累计总和 / 总次数的周期累计人均值 / 触发用户数的周期累计总和 / 触发用户数的周期累计人均值]，如抽卡 (draw_card)] 的 触发用户数 / 总次数 
  2.事件属性指标：[匹配到的事件属性，如事件属性为数值类型，可选 总和/人均值/周期累计总和/周期累计人均值；如事件属性为布尔，可选 为真数/为假数/为空数/不为空数]，如充值 (payment) 的 is_first_pay 为真数，表示回访用户中是第一次付费的次数
🔍 指标级筛选 (仅针对该展示指标)：设定 [事件属性，例：渠道 (channel)] [逻辑，例：等于/不等于/包括/不包括/有值/无值/正则匹配/正则不匹配] [具体值，例：官网]

## 使用关联属性 (进阶选填，用于极其精准的同类别回访)
(用于限定用户不仅要回访，且回访的具体对象必须与初始动作一致)
匹配逻辑：要求【初始事件】的 [匹配到的事件属性，例：活动类型] 等于 【回访事件】的 [匹配到的事件属性，例：活动类型] （若开启了使用同时展示，还要等于 同时展示的 匹配到的事件属性）

## 全局筛选 (选填，圈定分析的整体大盘)
选择过滤条件：[匹配到的 事件属性 / 公共事件属性 / 用户属性 / 用户标签 / 用户分群]
选择逻辑：[有值 / 无值 / 大于等于 / 小于等于 / 位于区间 / 等于 / 不等于 / 包括 / 不包括 等]
设定具体值：[具体数值或字符串]
示例：操作系统 (os) 等于 ios

## 分组项 (选填，拆解留存曲线)
(⚠️ 警告：若按事件属性分组，只能提取「初始事件」的事件属性)
按维度分组：选择 [匹配到的 初始事件事件属性 / 公共事件属性 / 用户属性 / 用户标签 / 用户分群]
示例：按 app_version (初始事件属性) 分组

## 时间范围与粒度
留存周期：按 [日 / 周 / 月] - [当日 / 次日 / 7日 / 14日 / N 日]
考察范围：[例如：昨日 / 今日 / 上周 / 本周 /上月 / 本月 / 过去7天 / 最近7天 等等]

📝 复杂实战示例参考 (Agent 学习库):
用户需求：查看不同app版本的近7天每日ios系统的「新用户留存」情况，同时显示官网的留存用户数。
分析主体: 账号
初始事件: register
回访事件: login
同时展示: login 的 触发用户数 + 指标筛选 (channel 等于 官网)
全局筛选: os (公共/用户属性) 等于 ios
分组项: app_version (此为 register 的事件属性)
时间与粒度: 按日-7日留存，时间范围：最近 7 天。
```
---

### 【漏斗分析 (funnel) 配置方法】

#### 前置数据映射要求 (Agent 必读):
配置漏斗模型前，必须查阅底层数据字典，确保事件序列的合理性：
- 多步骤事件映射：漏斗的每一个步骤都必须映射为一个真实的事件 (Event)。
- 单一事件的纵深漏斗：若业务需求是“同一行为的逐级转化”（如关卡1->关卡2->关卡3），需使用同一个事件，并在每个步骤内部叠加独立的事件属性筛选。

```
## 分析主体
选择转化追踪的视角：[匹配到的真实分析主体，例：用户 (user_id) / 角色 (role_id)/ 访客（distinct_id），若没有提示用户创建]

## 漏斗步骤 (核心流转配置)
(按业务逻辑依次添加必须顺序触发的事件，最多支持 30 步)
步骤 1 (起点)：选择 [匹配到的真实事件，例：注册 (register)]
- 步骤级筛选 (选填)：设定 [属性名] [逻辑] [具体值]
步骤 2：选择 [匹配到的真实事件，例：登录 (login)]
- 步骤级筛选 (选填)：设定 [属性名] [逻辑] [具体值]
步骤 N (终点)：选择 [匹配到的真实事件，例：通关 (level_pass)]
- 步骤级筛选 (选填)：设定 [匹配到的属性，例：关卡等级 (vip_level)] [逻辑，例：等于] [具体值，例：5]

## 分析窗口期 (转化时效)
设定用户从触发「步骤 1」开始，必须完成整个漏斗的时间限制。
窗口范围：建议根据业务常识设定 [最小1分钟，最大180天。例：1天 / 2小时 / 15分钟]。

## 使用关联属性 (进阶选填，用于商品/活动/对局的精准串联)
(不仅要求按顺序触发，还强制要求串联步骤中的某个核心属性值完全一致，属性为 null 则剔除)
匹配逻辑：要求各个步骤中的 [匹配到的核心事件属性，例：商品ID (item_id) / 活动ID (activity_id)] 完全相等。

## 全局筛选 (选填，圈定漏斗分析的大盘)
设定过滤条件：[匹配到的 事件属性 / 公共事件属性 / 用户属性 / 用户标签 / 用户分群]
选择逻辑：[有值 / 无值 / 大于等于 / 小于等于 / 位于区间 / 等于 / 不等于 / 包括 / 不包括 等]
设定具体值：[具体数值或字符串]
示例：操作系统 (os) 等于 ios

## 分组项 (选填，拆解转化率对比)
(按指定维度将漏斗拆分为多条，对比不同人群的转化能力)
按维度分组：选择 [匹配到的 步骤1的事件属性 / 公共事件属性 / 用户属性 / 用户标签 / 用户分群]（⚠️注：从步骤1的事件属性、用户属性、用户分群、用户标签中选择，至多一个添加为分组项）
示例：按 渠道 (channel) 分组

## 时间范围
考察漏斗触发起点的时间范围：[例如：上月 / 过去 7 天]

> 图表视角: [根据用户需求建议视角：若看整体流失情况选「转化图」 / 若看每日转化率波动选「趋势图」]

📝 复杂实战示例参考 (Agent 学习库):
用户需求：想看上个月不同渠道的 iOS 用户，1天内从注册、登录、升级、参加活动、抽卡到登出的整个转化漏斗。
分析主体: 账号 (user_id)
漏斗步骤:
register (注册)
login (登录)
level_up (升级)
activity_attend (参加活动)
draw_card (抽卡)
logout (登出)
分析窗口期: 1 天
全局筛选: os 等于 ios
分组项: channel (渠道)
时间范围: 上月

```
---

### 【间隔分析 (interval) 配置方法】

#### 前置数据映射与计算法则 (Agent 必读):
- 底层计算机制（必须深刻理解）:
  - 不同事件（A -> B）采用最短间隔原则：若序列为 A1->A2->B1->B2，仅计算 A2->B1 的一次间隔。
  - 相同事件（A -> A）采用相邻间隔原则：若序列为 A1->A2->A3，会产生 A1->A2 和 A2->A3 两次间隔。
- 属性层级约束（极易出错，必须遵守）：分组项如果选择事件属性，必须且只能使用「起点事件」的事件属性。绝不能拿终点事件的属性用于全局拆解！

```
## 分析主体
选择计算的视角：[匹配到的真实分析主体，例：用户 (user_id) / 角色 (role_id)/ 访客（distinct_id），若没有提示用户创建]
 (注：起点和终点事件必须由同一主体触发才算一次完整间隔)

## 起点事件与终点事件 (支持独立指标级筛选)
🟢 起点事件：选择 [匹配到的真实事件，例：充值 (payment)]
指标级筛选 (选填)：设定 [属性名，例：充值金额 (pay_amount)] [逻辑，例：等于] [具体值，例：0]
🛑 终点事件：选择 [匹配到的真实事件，例：充值 (payment) 或 登录 (login)]
指标级筛选 (选填)：设定 [属性名，例：充值金额 (pay_amount)] [逻辑，例：大于等于] [具体值，例：100]

## 间隔上限 (剔除无效长尾数据)
设定最大有效计算时长：[根据业务常识设定，最小1分钟，最大180天。例：1 天 / 2 小时] (注：超过此上限的跨度数据将被直接排除)

## 使用关联属性 (进阶强约束，用于同类目/连续动作匹配)
(不仅要求按时间先后触发，还强制要求两者某属性关联。属性为 null 则剔除)
常规匹配：要求【起点事件】的 [匹配到的事件属性，例：账户ID (account_id)] 等于 【终点事件】的 [匹配到的事件属性]，两者的属性类型必须一致。
高阶数值匹配 (差值计算)：若为数值型属性，可设置偏差。例如要求【终点事件】的 [关卡ID (level_id)] 比【起点事件】的该属性 大 1（用于剔除重复刷同一关卡的干扰，只看真实晋级耗时）。

## 全局筛选 (选填，圈定分析的大盘)
设定过滤条件：[匹配到的 事件属性 / 公共事件属性 / 用户属性 / 用户标签 / 用户分群]
选择逻辑：[有值 / 无值 / 大于等于 / 小于等于 / 位于区间 / 等于 / 不等于 / 包括 / 不包括 等]
设定具体值：[具体数值或字符串]
示例：操作系统 (os) 等于 ios

## 分组项 (选填，拆解不同人群的耗时差异)
(⚠️ 警告：若按事件属性分组，只能提取「起点事件」发生那一刻的属性值)
按维度分组：选择 [匹配到的 起点事件事件属性 / 公共事件属性 / 用户属性 / 用户标签 / 用户分群]
示例：按 起点事件的 渠道 (channel) 分组

## 时间范围与粒度
展现维度：按 [天 / 小时 / 周 / 月 / 合计]
时间范围：[例如：本月 / 过去 7 天]

> 图表与指标视角建议:
  - [若用户想看最大/小值、平均值、中位数等聚合统计，建议选「盒须图 (Box Plot)」]
  - [若用户想看耗时区间分布，比如多少人花了0-5分钟，多少人花了5-10分钟，建议选「直方图 (Histogram)」，并可提示自定义区间边界]

📝 复杂实战示例参考 (Agent 学习库):
用户需求：想看本月每天不同渠道的 iOS 用户，同一账号ID在1天内从付费金额为0到付费金额大于等于100的耗时间隔。
分析主体: 用户
起点事件: payment + 筛选 pay_amount 等于 0
终点事件: payment + 筛选 pay_amount 大于等于 100
间隔上限: 1 天
关联属性: 起点事件的 账户ID 等于 终点事件的 账户ID
全局筛选: os 等于 ios
分组项: channel (必须是起点事件的属性)
时间粒度与范围: 按天展示，本月
```

---

### 【分布分析 (distribution/scatter) 配置方法】

#### 前置数据映射与计算法则 (Agent 必读):
配置分布模型前，必须查阅底层数据字典，并深刻理解以下嵌套计算逻辑：
- 聚合属性的数据类型约束：若按事件属性聚合，文本型属性仅支持「去重数」；数值型属性支持「总和/均值/中位数/最值/分位数/方差等」。绝不能对文本属性使用求和或均值！
- 多层级筛选（极易混淆，必须严格区分）：
  - 事件级筛选：挂载在具体的某一个事件上（如：仅计算 is_first_pay=真 的 payment 次数）。
  - 指标级筛选 (全局)：挂载在整个完整指标或公式的外部（如：(A/B) 结果计算完后，整体只看 渠道=官网 的数据）。

```
## 分析主体
选择计算的底层视角：[匹配到的真实分析主体，例：用户 (user_id) / 角色 (role_id)] (注：所有后续频次和数值都将基于该主体进行汇总)

## 参与事件 (分层依据 / 核心指标构建)
(支持以下三种模式构建分层依据，并在每个事件上支持独立的「事件筛选」)
### 模式 A：基于行为频次分层
选择 [匹配到的真实事件，例：登录 (login)]
指标级筛选 (选填)：[匹配到的 事件属性 / 公共事件属性 / 用户属性 / 用户标签 / 用户分群，例：is_first_login] [逻辑] [具体值，例：为真]
聚合口径：[次数 / 天数 / 小时数]
### 模式 B：基于事件属性的计算分层
选择 [匹配到的真实事件，例：充值 (payment)]
选择 [匹配到的事件属性]。(若为文本类型，只有「去重数」；若为数值型，可选「总和/均值/中位数/最大值/最小值/方差/标准差/分位数等」)
指标级筛选 (选填)：同上
聚合口径：[次数 / 天数 / 小时数]
### 模式 C：基于公式的高级计算分层
利用基础事件组合公式，例如：[事件A 的 聚合口径] / [事件B 的 聚合口径]。每个事件都可以拥有独立的事件级筛选。
事件级筛选 (选填)：[匹配到的属性，例：is_first_login] [逻辑] [具体值，例：为真]

## 区间划分 (分桶规则)
根据算出的指标分布情况设定数据区间，支持默认区间、离散数字和自定义区间。[例如：根据常理推断的区间边界，如 0-1, 1-5, 5-10, 大于10]

## 使用同时展示 (进阶选填，透视特定区间人群的价值)
(在区间划分完毕后，进一步看落入该区间的这批人的其他表现)
参与事件：[匹配到的价值事件，例：充值 (payment)]
分析指标：[总次数 / 触发用户数 / 人均次数 / 事件属性的数值聚合等]。也可利用基础事件组合高级公式，同上面模式C。
事件级筛选 (选填)：[匹配到的属性，例：is_first_login] [逻辑] [具体值，例：为真]
指标级筛选 (仅针对该展示指标)：设定 [属性名] [逻辑] [具体值]

## 分组项 (选填，拆解不同维度的分布结构)
按维度拆分区间结构：选择 [匹配到的 事件属性 / 公共事件属性 / 用户属性 / 用户标签 / 用户分群]

## 时间范围与展现形式
时间展现粒度：按 [天 / 周 / 月 / 合计]
时间考察范围：[例如：本月 / 过去 7 天 等]

> 图表与视角建议:
  - [若需看最详尽的数据对比（或有分组项），建议优先查看「表格」]
  - [若需看整体大盘的静态分布，建议选「直方图 (Histogram) / 柱状图」]
  - [若需看分布结构随时间的变化趋势，建议选「百分比分布 / 数值分布」]

📝 复杂实战示例参考 (Agent 学习库):
用户需求：想看本月每天各个渠道用户首次付费次数分布，同时展示所有付费人数。
分析主体: 用户
参与事件 (分层依据): payment
聚合口径：次数
事件级筛选: is_first_pay (首充状态) 为 真
使用同时展示: payment 的 触发用户数 (即付费人数)
分组项: channel (渠道)
区间划分: 根据业务自动或手动设定的次数区间 (如 1次, 2次等)
```

---

### 【路径分析 (path) 配置方法】

#### 前置数据映射与严格约束 (Agent 必读):
配置路径模型前，必须查阅底层数据字典，并死死记住以下两条红线规则：
- 绝对禁止事件级筛选：选定的参与事件不可附加任何筛选条件（如：不能只看 pay_amount>0 的 payment，只能把整个 payment 拉进来）。
- 过滤维度的降维打击：本模型的全局筛选叫做「用户筛选」，只接受用户表里的字段（用户属性/分群/标签）。绝不能使用任何事件属性（包含公共事件属性）来做过滤！

```
## 参与分析的事件 (构建轨迹的基础节点)
挑选构建行为轨迹的候选动作集合（最多30个元事件）。
选定范围：[匹配到的核心事件列表，例：登录 (login), 升级 (level_up), 抽卡 (draw_card), 登出 (logout) 等]
(⚠️ 警告：此处仅选择事件名，绝对不可附加任何筛选条件！)

## 事件拆分 (进阶选填，用于细化同类动作的具体流向)
(将同一个事件，根据其某个属性的值，裂变成桑基图上的多个不同节点)
拆分目标：选择 [上述已选列表中的某个事件，例：升级 (level_up)]
拆分依据：按 [匹配到的该事件的事件属性，例：等级 (level)] 拆分。
(呈现效果：图表上原本单一的“升级”节点，将裂变为“升级(level=1)”、“升级(level=2)”等独立节点。)

## 分析路径的起点/终点 (确定探索方向)
(根据业务需求，二选一设定)
正向探索 (看某动作之后的流向)：选择 [匹配到的特定事件，例：登录 (login)] 作为 「初始事件」。系统将向后追踪。
逆向追溯 (看流失或转化前的行为)：选择 [匹配到的特定事件，例：登出 (logout)] 作为 「结束事件」。系统将向前追溯。

## 会话间隔时长 (Session 切割刀)
(设定相邻两个动作被视为同一次连续会话的最大超时时间)
间隔上限：建议 [根据业务推断，最小1秒，最大24小时。例：30 分钟 / 2 小时]。
(注：超过此间隔未发生下一个所选事件，则视为本次会话中断，轨迹结束。)

## 用户筛选 (全局过滤大盘)
(⚠️ 警告：只能从用户属性、用户标签、用户分群中选择！不支持单一事件属性筛选)
设定用户过滤条件：[匹配到的 用户属性 / 用户标签] [逻辑] [具体值]；或 [匹配到的用户分群] [属于分群/不属于分群]。
示例：中R 属于分群。

## 时间范围
考察用户轨迹的时间窗口：[例如：过去 7 天 / 上月]

> 图表解读建议:
  - 提示用户查看生成的**桑基图 (Sankey Diagram)**
  - 矩形块的高度代表流量大小，连线代表流转比例。点击特定节点可高亮查看流经该节点的专属轨迹

📝 复杂实战示例参考 (Agent 学习库):
用户需求：想看过去7天中R用户从登录后的行为路径，并将升级事件按照等级拆分，相邻两次行为不超过30分钟。
参与分析的事件: login (登录), gold_get (获取金币), draw_card (抽卡), level_up (升级), logout (登出) 等
事件拆分: 将 level_up 事件，按 level (等级) 属性拆分
分析路径起点: 以 login 作为 初始事件 (正向探索)
会话间隔时长: 30 分钟
用户筛选: 选择用户分群 中R，逻辑为 属于分群
时间范围: 过去 7 天
```

---

### 【属性分析 (composition) 配置方法】

#### 前置数据映射与严苛红线 (Agent 必读):
配置属性模型前，必须查阅底层数据字典，并死死记住以下三条红线：

- 绝对禁区：**严禁使用任何「事件 (Event)」或「事件属性 / 公共事件属性」**！ 本模型只能查询用户字典中的数据（用户属性、用户标签、用户分群）。
- 时区与版本无关性：用户属性没有时区概念（不可选展示时区）；用户标签强制使用系统的最新版本进行计算。
- 聚合类型的强依赖：作为指标分析时，计算公式严格受限于该用户属性的数据类型（详见下方配置步骤）。

```
## 分析指标 (画像的评估基准)
(根据需求评估基准，选择以下配置之一，注意只能分析指标只能有一个，不可添加多个指标)
默认选项：选择 [用户数]（即符合条件的 #user_id 的去重数）。
进阶选项：基于用户属性/标签计算：
选择 [匹配到的用户属性 / 用户标签，例：累计充值金额 (total_pay_amount)]，并根据其数据类型选择合法的计算口径：
  - 数值型：[总和 / 均值 / 人均值 / 中位数 / 最大值 / 最小值 / 去重数 / 方差 / 标准差]
  - 文本/时间型：[去重数]
  - 布尔型 (仅属性)：[为真数 / 为假数 / 为空数 / 不为空数 / 去重数]
  - 列表型 (仅属性)：[列表去重数 / 集合去重数 / 元素去重数]
  - 对象/对象组型 (仅属性)：[去重数 / 为空数 / 不为空数]

## 全局筛选 (圈定整体画像底池)
(⚠️ 警告：仅限使用用户属性/用户标签/用户分群字段，不可用事件属性)
设定用户过滤条件：
- 选用户属性/用户标签：[匹配到的用户属性/标签] [逻辑] [具体值 / 字符串]
- 选用户分群：[匹配到的用户分群，例：中R用户] [逻辑：属于分群 / 不属于分群]

## 分组统计 (双模式画像拆解，二选一)
(根据业务是要“看内部构成”还是“看外部对比”，选择分组模式)
【模式 A：按「属性」进行分组统计】(看大盘内部构成与交叉)
选择 1 到 2 个 [匹配到的 用户属性 / 用户标签 / 用户分群]。
(Agent 提示：若选 1 个，直观呈现分布比例；若选 2 个，呈现为堆积柱状图或多维交叉表格。)
示例：按 省份 (province) 分组。
【模式 B：按「人群」进行分组统计】(看不同客群的特征对比)
自定义圈定最多 10 组人群进行横向对比。
组 1：[例：全部用户 (作为对照大盘)]
组 2：设定 [匹配到的用户属性/用户标签/用户分群] 条件圈出的人群 [例：高价值流失预警分群]
组 3：设定 ... 条件圈出的人群。

> 图表解读建议:
  - [若需看不同维度的绝对值差异，建议选「柱状分布」]
  - [若需看各部分占大盘的比例，建议选「饼状分布」]
  - [若配置了两个分组项，建议直接查看「表格」获取多维交叉数据]
  - ⚠️ 注意事项: 属性分析针对的是用户的静态画像（用户表），无时区概念，且无法分析具体的动态行为事件（如"昨日充值"请使用事件分析）。

📝 复杂实战示例参考 (Agent 学习库):
用户需求：想看各个省份的中R用户数。
分析指标: 用户数 (默认的 #user_id 去重数)
全局筛选: 中R用户 (用户分群)，逻辑为 属于分群
分组统计模式: 按「属性」进行分组统计
分组项: province (用户属性)

用户需求对比示例：想看「全部用户」与「核心活跃分群」的累计付费金额人均值是否存在差异？
分析指标: total_pay_amount (用户属性，数值型)，计算口径选 人均值
分组统计模式: 按「人群」进行分组统计
对照人群设置:
人群 1: 全部用户
人群 2: 筛选条件为属于 核心活跃分群
```

---

### 【归因分析 (attribution) 配置方法】

#### 前置数据映射与严苛红线 (Agent 必读):
配置归因模型前，必须查阅底层数据字典，并死死记住以下几条计算红线：
- 目标事件的聚合限制：转化价值只能是两种口径：事件的**「总次数」，或者该事件某个数值型属性的「总和」**。绝对不能使用去重数、均值等逻辑！
- 分组对象的隔离机制：
  - 目标事件的分组：拆分的是大盘转化总额（例：按地区分组，看各地区的充值金额分别被谁邀功了）。
  - 归因事件的分组：拆分的是触点（例：按渠道分组，看某个活动位具体是哪个渠道贡献的）。

```
## 分析主体
选择贯穿触点与转化的唯一识别标识：[匹配到的真实分析主体，例：用户 (user_id) / 访客 (distinct_id)，若没有提示用户创建]

## 归因方式 (根据业务目标选择分配逻辑)：
[首次归因]：100% 功劳给窗口内第一个触点（适合评估拉新/破冰）。
[末次归因]：100% 功劳给窗口内最后一个触点（适合评估临门一脚促转化）。
[线性归因]：窗口内所有有效触点平分功劳（适合评估长效种草）。

## 窗口期 (触点的保质期，设定从目标事件向前追溯触点的时间范围)：
[当天]：回溯至目标事件发生当天的 0:00。
[自定义]：[根据业务推断设定固定时长，例：1小时 / 3天]。

## 目标事件 (定义总奖金池)
(我们要把什么价值分发出去？)
- 转化事件：选择 [匹配到的最终转化事件，例：充值 (payment)]
- 转化价值 (严苛约束)：[总次数] 或 [匹配到的数值型事件属性，例：支付金额 (pay_amount) 的 总和]
- 事件级筛选 (选填)：设定 [事件属性] [逻辑] [具体值/字符串]
- 目标事件分组 (选填)：按照 [匹配到的属性，例：省份 (province)] 分组，以分别查看不同群体带来的贡献总池。
- 直接转化参与归因计算
[Agent 强烈建议勾选]：若目标事件在窗口期内找不到任何归因事件，该转化价值将被计入「直接转化」（即自然流量/无明确触点转化），避免盲目拔高现有触点的贡献度。

## 归因事件 (定义争夺奖金的触点，可添加多个)
(谁来分走功劳？)
触点 1：选择 [匹配到的触点事件，例：点击拍脸图 (click_banner)]
- 事件级筛选 (选填)：同上。
- 归因事件分组 (选填)：按照 [匹配到的触点事件属性，例：注册渠道 (channel)] 分组。
触点 2：选择 [匹配到的触点事件，例：充值活动icon点击 (click_recharge_icon)]...
- 关联属性 (进阶强约束，用于精准触点匹配)
(确保触达的对象和购买的对象是同一个东西，属性为 null 则剔除)
  匹配逻辑：要求【归因事件】的 [匹配到的核心事件属性，例：商品ID (item_id)] 等于 【目标事件】的对应属性。

## 全局筛选 (圈定分析的大盘)
设定过滤条件：[匹配到的 事件属性 / 用户属性 / 用户标签 / 用户分群] [逻辑] [具体值]
示例：用户地区 有值。

## 时间范围
目标事件发生的时间窗口：[例如：过去 7 天 / 本月]

> 核心指标解读: 重点关注各触点的 [对目标事件的贡献值 (绝对值)] 和 [贡献度 (百分比)]，以及 [有效触发率]。

📝 复杂实战示例参考 (Agent 学习库):
用户需求：查看最近7天用户地区有值情况下，不同注册渠道的拍脸图点击和不同注册渠道的充值活动icon点击，为各个地区带来了多少付费总金额（假设用末次归因，1小时窗口）。
分析主体: 用户
归因方式与窗口: 末次归因，自定义 1 小时
目标事件 (产出): payment 事件的 pay_amount 的 总和。【目标事件分组】: 按 用户地区 (province/city) 分组。
直接转化: 勾选 (直接转化参与归因计算)
归因事件 1 (触点): 拍脸图点击事件。【归因事件分组】: 按 渠道 (channel) 分组。
归因事件 2 (触点): 充值活动icon点击事件。【归因事件分组】: 按 渠道 (channel) 分组。
全局筛选: 用户地区 (province/city) 逻辑为 有值
时间范围: 最近 7 天
```

---

### 【排行榜 (rank) 配置方法】

#### 前置数据映射与严苛红线 (Agent 必读):
配置排行榜模型前，必须查阅底层数据字典，并死死记住以下红线：
- 数据关联的铁律：作为「排名指标」和「同时展示指标」的事件，必须包含所选「榜单主体」对应的属性字段！否则无法进行数据聚合。
- 名次处理的 SQL 映射：必须根据业务对“同分”的容忍度，准确映射底层的三种排序逻辑（rank, dense_rank, row_number）。

```
## 榜单主体 (对谁进行排名)
(这是排行的核心维度，默认是账户ID)
选择维度：[匹配到的 用户属性 / 事件属性，例：账户ID (#account_id) / 关卡ID (level_id) / 话题 (topic)]

## 排名指标 (排名的数值依据)
(基于榜单主体相关的事件，构建排序核心分数)
参与事件：[匹配到的真实事件，例：充值 (payment)]
聚合口径：
  - 预置口径：[总次数 / 触发用户数 / 人均次数]
  - 数值属性口径：选择 [匹配到的数值型事件属性] 的 [总和 / 均值 / 最大值 / 最小值 等]
排序方向：[降序 (Desc，默认) / 升序 (Asc)]

## 并列名次处理 (同分时的仲裁规则)
(严格根据业务场景，三选一)
- [并列且跳过 (rank)]：支持并列，后续名次断层（如：1, 2, 2, 4）。
- [并列不跳过 (dense_rank)]：支持并列，后续名次连续（如：1, 2, 2, 3）。
- [按默认排序 (row_number)]：不支持并列，强制分出先后（如：1, 2, 3, 4。注：底层按字典序辅助排序）。

## 同时展示指标 (进阶选填，补充榜单主体的其他表现)
(不参与排序，但与排行榜并行展示。支持公式与多层筛选)
参与事件与口径：[匹配到的事件及计算方法，同上]
自定义公式：支持基础指标的四则运算。
事件级筛选：设定 [属性名] [逻辑] [具体值]（仅作用于公式中的事件）。
指标级筛选：设定 [属性名] [逻辑] [具体值]（作用于整个展示指标）。

## 全局筛选 (选填，圈定生成榜单的大盘范围)
设定过滤条件：[匹配到的 事件属性 / 用户属性 / 用户标签 / 用户分群] [逻辑] [具体值]

## 排名时段与对比 (看涨跌幅必填)
当前时间范围：[例如：过去 7 天 / 本周]
对比时段 (选填)：若需计算“名次浮动（如上升2名，下降3名）”，需设定对比基准。[例如：上一阶段]

> 图表解读建议: 提示用户重点关注开启了「对比时段」后的**名次变化箭头（↑/↓），以快速定位上升或下降最快的业务主体。

📝 复杂实战示例参考 (Agent 学习库):
用户需求：想看过去7天官网各个账号的付费总金额排名（不支持并列），同时展示他们在官网的付费总次数。
榜单主体: 账户ID (#account_id)
排名指标: payment (付费事件) 的 pay_amount (支付金额) 的 总和，降序。
并列名次处理: 按默认排序 (row_number)
同时展示指标: payment 的 总次数。
全局筛选: channel (渠道) 等于 官网
时间范围: 过去 7 天
```

---

### 【热力地图 (heatmap) 配置方法】

#### 前置数据映射与严苛红线 (Agent 必读):
配置热力图模型前，必须查阅底层数据字典，并死死记住以下红线：
- 坐标属性的刚性依赖：选择的热力事件（或该事件关联的用户），必须具备代表 X 轴和 Y 轴坐标的数值型属性，否则绝对无法生成地图！
- 极值/首末次过滤的“单用户”原则：首末次过滤是针对“每个 #user_id”进行的降维打击，确保每个玩家在所选窗口期内只贡献 1 条数据（首次/末次/最大/最小），防止单点刷量干扰整体热度分布。
- 数据类型决定筛选逻辑：事件筛选的运算符严格受限于属性类型（如文本型支持正则，对象组支持集合判断）。

```
## 热力指标 (定位动作并决定热力值)
热力事件：选择 [匹配到的空间交互事件，例：角色死亡 (player_dead) 或 开启宝箱 (open_chest)]。
事件筛选 (选填)：支持多个条件并设定 [且 (AND) / 或 (OR)] 关系。根据筛选属性类型，运算符必须遵守底层类型约束：
  - 文本型：[等于 / 不等于 / 包括 / 不包括 / 有值 / 无值 / 正则匹配 / 正则不匹配]
  - 时间型：[位于区间]
  - 数值型：[等于 / 不等于 / 小于 / 大于 等]
  - 对象型 (Object)：[有值 / 无值]
  - 对象组型 (Object Array)：[存在对象满足 / 没有对象满足 / 全部对象满足 / 有值 / 无值]
计算方式：(决定颜色深浅)：
  - 若看频次/人数：选 [总次数 / 触发用户数 / 人均次数]。
  - 若看具体数值：选择 [匹配到的数值型属性，例：资源获取量 (resource_amount)] 的 [总和 / 均值 / 最大值 / 最小值 / 去重数 等]。
首末次过滤 (进阶选填，用于消除单玩家重复刷量或定位极值)
(开启后，每个用户在整个时间窗口内只保留 1 条事件参与热力计算)
设定仅保留每个用户在该时间窗口内的 [首次 / 末次 / 某属性最大值 / 某属性最小值] 的那一条记录参与渲染。
过滤方式 (四选一)：
  - [首次 / 末次]：保留该用户时间窗口内的第一条或最后一条记录。
  - [最大 / 最小]：选择某个 [数值或时间类型属性]，保留该属性达到极值的那一条记录。

## 事件坐标 (灵魂绑定)
X轴属性：必须绑定字典中匹配的 [数值型坐标属性，例：pos_x 或 screen_width]。
Y轴属性：必须绑定字典中匹配的 [数值型坐标属性，例：pos_y 或 screen_height]。
(注：可使用事件属性或用户属性)

## 地图文件
请选择已有地图或上传新地图（⚠️注：若上传新地图，需提醒用户输入该地图左下角与右上角的真实极限坐标点进行底图校准）。

## 多组对比 (如有对照需求，选填，2-4组)：用于左右平铺对比不同人群的热力差异。
组 1：[例：条件为空，代表全部数据]
组 2：设定过滤条件 [例：用户分群 = 高段位玩家]。
(🔥 隐藏高阶技巧：若要对比不同时间段（如上周 vs 本周），可在各组的筛选条件中使用 #event_time (事件时间) 进行分别限定！)

## 时间范围
设定全局的分析窗口：[例如：过去 7 天 / 本月]

## 图表交互建议:
  - 提示用户可利用右下角控件调节「热力半径」和「透明度」以防止热力点糊成一团。
  - 若开启了多组对比，强烈建议提醒用户勾选「同步缩放」，以便于对齐查看特定局部地图（如某特定草丛/BOSS房间）的差异。

📝 复杂实战示例参考 (Agent 学习库):
用户需求：作为地图策划，我想对比过去 7 天，「新手玩家」和「老玩家」在地图内「首次发生战斗」的位置分布，看他们遭遇战的聚集点有何不同；同时排除掉那些使用特定非法挂机脚本（用正则匹配设备品牌包含"emulator"）的记录。
热力事件: battle_occur (发生战斗)
事件筛选: brand (设备品牌) 正则不匹配 .*emulator.*
首末次过滤: 开启，选择 [首次] (每个玩家只取第一场战斗)。
计算 (热力指标): 触发用户数 (看哪个地方卷入战斗的人最多)。
事件坐标: X轴 = pos_x，Y轴 = pos_y。
多组对比:
组 1 (新手): 筛选 user_level (用户等级) < 10
组 2 (老手): 筛选 user_level (用户等级) >= 10
时间范围: 过去 7 天
```

---

### 【SQL 查询 (sql) 方法】

- 🎯 **查询目标**: 简要说明这段 SQL 解决了什么其他模型无法解决的复杂逻辑（例：跨项目查询 / 复杂多表 JOIN / 特殊自定义口径）。
- 📝 **SQL 语句**:
(Agent 内部指令：根据底层数据字典，输出可以直接在 TE 中运行的 Trino SQL。必须严格遵守：字段名含特殊符号如 $ 或 # 必须用双引号 `""`，字符串值必须用单引号 `''`。默认事件表为 `ta.v_event_1`，用户表为 `ta.v_user_1`，除非明确了其他项目ID。)

```sql
-- 示例格式：
SELECT
  "$part_date",
  COUNT(DISTINCT "#user_id") AS "user_count"
FROM ta.v_event_1
WHERE "$part_event" = '[匹配到的事件名]'
  AND "[匹配到的属性名]" = '具体值'
GROUP BY "$part_date"
ORDER BY "$part_date" ASC
```

---

## 多模型组合建议

在实际工作中，复杂分析场景往往需要多个模型组合使用。遇到复杂问题时，可以建议模型组合方案：

**示例 — 分析"次日留存率下降"**:
1. **留存分析** → 先确认次留率下降的幅度和时间点
2. **事件分析** → 对比留存和流失用户的关键行为指标差异
3. **漏斗分析** → 检查新手引导流程的转化率是否下降
4. **路径分析** → 探索流失用户离开前的行为路径
5. **分布分析** → 查看流失用户的在线时长/使用次数分布

**示例 — 分析"付费转化优化"**:
1. **漏斗分析** → 找到付费流程中流失最大的步骤
2. **间隔分析** → 了解用户从浏览到付费的决策时长
3. **路径分析** → 发现付费用户的典型行为路径
4. **归因分析** → 评估不同触点对付费的贡献
